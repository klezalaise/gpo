'use strict'

let MODULE_MONTURES = (function () {

  let module = {}

  // vue des montures
  module.MontureVue = Vue.extend({
      template: '<div id="montures">Liste des montures</p>'
  })

	return module;

}());
